<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0 http://maven.apache.org/xsd/assembly-1.1.0.xsd">
  <id>bin</id> <!-- This assembly packages the webstart jars and jnlp files to be merged into the webapp hierarchy -->
  <baseDirectory>/</baseDirectory>
  <formats>
    <format>zip</format>
  </formats>
  <fileSets>
    <fileSet>
      <directory>${project.build.directory}/${project.artifactId}-${project.version}/lib</directory>
      <outputDirectory>/lib</outputDirectory>
      <excludes>
        <exclude>*.jnlp</exclude>
        <exclude>*.xml</exclude>
      </excludes>
    </fileSet>
    <fileSet>
      <directory>${project.build.directory}/${project.artifactId}-${project.version}/lib</directory>
      <outputDirectory>/docs/jnlp</outputDirectory>
      <includes>
        <include>*.jnlp</include>
        <include>*.xml</include>
      </includes>
    </fileSet>
  </fileSets>
</assembly>