<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <artifactId>tourapp-tour-acctpay-reactor</artifactId>
    <groupId>com.tourapp.tour</groupId>
    <version>0.8.0</version>
  </parent>

  <artifactId>com.tourapp.tour.acctpay.screen</artifactId>
  <name>tourapp-tour-acctpay-screen - Accounts payable screen</name>
  <packaging>bundle</packaging>

  <dependencies>
    <dependency>
      <groupId>com.tourapp.tour</groupId>
      <version>${tourapp-version-range}</version>
      <artifactId>com.tourapp.tour.booking.entry</artifactId>
    </dependency>
    <dependency>
      <groupId>com.tourapp.tour</groupId>
      <artifactId>com.tourapp.tour.acctpay.db</artifactId>
      <version>${tourapp-version-range}</version>
    </dependency>
    <dependency>
      <groupId>com.tourapp.tour</groupId>
      <artifactId>com.tourapp.tour.acctpay.report</artifactId>
      <version>${tourapp-version-range}</version>
    </dependency>
    <dependency>
      <groupId>com.tourapp.tour</groupId>
      <artifactId>com.tourapp.tour.acctrec.db</artifactId>
      <version>${tourapp-version-range}</version>
    </dependency>
    <dependency>
      <groupId>com.tourapp.tour</groupId>
      <artifactId>com.tourapp.tour.acctrec.screen</artifactId>
      <version>${tourapp-version-range}</version>
    </dependency>
    <dependency>
      <groupId>com.tourapp.tour</groupId>
      <artifactId>com.tourapp.tour.genled.screen</artifactId>
      <version>${tourapp-version-range}</version>
    </dependency>
    
    <dependency>
      <groupId>com.tourapp.model</groupId>
      <version>${tourapp-version-range}</version>
      <artifactId>com.tourapp.model.tour.acctpay</artifactId>
    </dependency>
    
    <dependency>
      <groupId>com.tourapp.res</groupId>
      <artifactId>com.tourapp.res.tour.acctpay</artifactId>
      <version>${tourapp-version-range}</version>
    </dependency>
  </dependencies>

</project>
