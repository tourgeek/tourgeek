<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <artifactId>tourapp-tour-message-reactor</artifactId>
    <groupId>com.tourapp.tour</groupId>
    <version>0.8.2-SNAPSHOT</version>
  </parent>

  <artifactId>com.tourapp.tour.message.process</artifactId>
  <name>tourapp-tour-message-process</name>
  <description>Message processes</description>
  <packaging>bundle</packaging>

  <properties>
    <jbundle-package-export>
    com.tourapp.tour.message.*.in;version=${project.version},
    com.tourapp.tour.message.*.out;version=${project.version},
    com.tourapp.tour.message.*.screen;version=${project.version},
    </jbundle-package-export>
  </properties>

  <dependencies>
    <dependency>
	  <groupId>org.jbundle.config.dep</groupId>
	  <artifactId>jbundle-config-dep-base</artifactId>
	  <version>${jbundle-version-range}</version>
	  <type>pom</type>
    </dependency>

    <dependency>
      <groupId>org.jbundle.base.message</groupId>
      <artifactId>org.jbundle.base.message.jibx</artifactId>
      <version>${jbundle-version-range}</version>
    </dependency>

    <dependency>
      <groupId>com.tourapp.tour</groupId>
      <artifactId>com.tourapp.tour.message</artifactId>
      <version>${tourapp-version-range}</version>
    </dependency>

    <dependency>
      <groupId>com.tourapp.tour</groupId>
      <artifactId>com.tourapp.tour.acctpay.db</artifactId>
      <version>${tourapp-version-range}</version>
    </dependency>
    <dependency>
      <groupId>com.tourapp.tour</groupId>
      <artifactId>com.tourapp.tour.booking.db</artifactId>
      <version>${tourapp-version-range}</version>
    </dependency>
    <dependency>
      <groupId>com.tourapp.tour</groupId>
      <artifactId>com.tourapp.tour.booking.remote</artifactId>
      <version>${tourapp-version-range}</version>
    </dependency>
    <dependency>
      <groupId>com.tourapp.tour</groupId>
      <artifactId>com.tourapp.tour.product.db</artifactId>
      <version>${tourapp-version-range}</version>
    </dependency>
    <dependency>
      <groupId>com.tourapp.tour</groupId>
      <artifactId>com.tourapp.tour.product.screen</artifactId>
      <version>${tourapp-version-range}</version>
    </dependency>
  </dependencies>
</project>
